[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
markers = [
    "integration: requires the live cluster (Postgres/MinIO/NATS/catalog/Argo reachable) — excluded by default, run explicitly with `-m integration`.",
]
addopts = "-m 'not integration'"
